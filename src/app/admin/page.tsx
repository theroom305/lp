export default function AdminPage() {
  return (
    <main className="admin-shell" data-test-id="admin-placeholder">
      <p>Admin shell — placeholder. Full UI with Google OAuth lands in Step 5.</p>
    </main>
  );
}
